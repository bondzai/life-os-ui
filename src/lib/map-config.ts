// Shared map defaults — mapcn handles tiles, themes, and marker rendering
// Coordinates are [lng, lat] (MapLibre/GeoJSON convention)

export const DEFAULT_CENTER: [number, number] = [100.5018, 13.7563] // Bangkok
export const DEFAULT_ZOOM = 12
